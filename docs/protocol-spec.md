# UART Update Protocol Spec

TBD — will document:
- Packet format (start byte, length, payload, CRC)
- Command set (erase, write, jump-to-app, get-version)
- Error handling (bad CRC, timeout, invalid address range)
